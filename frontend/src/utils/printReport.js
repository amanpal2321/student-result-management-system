export function printResultReport(result) {
  const printWindow = window.open("", "_blank", "width=900,height=700");

  if (!printWindow) {
    window.alert("Please allow popups to print the report card.");
    return;
  }

  const html = `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Report Card - ${result.studentName}</title>
  <style>
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: "Segoe UI", Tahoma, sans-serif;
      background: #f8fafc;
      color: #0f172a;
      padding: 32px;
    }
    .sheet {
      max-width: 860px;
      margin: 0 auto;
      background: white;
      border: 1px solid #cbd5e1;
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 20px 60px rgba(15, 23, 42, 0.12);
    }
    .hero {
      background: linear-gradient(135deg, #0f172a, #164e63 65%, #22d3ee);
      color: white;
      padding: 32px;
    }
    .hero h1 {
      margin: 0 0 8px;
      font-size: 32px;
    }
    .hero p {
      margin: 0;
      color: rgba(255, 255, 255, 0.85);
    }
    .content {
      padding: 28px 32px 32px;
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 16px;
      margin-bottom: 24px;
    }
    .card {
      border: 1px solid #e2e8f0;
      border-radius: 18px;
      padding: 18px;
      background: #f8fafc;
    }
    .label {
      font-size: 12px;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: #475569;
      margin-bottom: 10px;
    }
    .value {
      font-size: 24px;
      font-weight: 700;
      color: #0f172a;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 12px;
      overflow: hidden;
      border-radius: 18px;
      border: 1px solid #e2e8f0;
    }
    thead {
      background: #e2e8f0;
    }
    th, td {
      padding: 14px 16px;
      text-align: left;
      border-bottom: 1px solid #e2e8f0;
    }
    .badge {
      display: inline-flex;
      align-items: center;
      padding: 8px 14px;
      border-radius: 999px;
      font-weight: 700;
      background: ${result.grade === "A" ? "#dcfce7" : result.grade === "B" ? "#fef3c7" : "#fee2e2"};
      color: ${result.grade === "A" ? "#166534" : result.grade === "B" ? "#92400e" : "#991b1b"};
    }
    .footer {
      margin-top: 28px;
      display: flex;
      justify-content: space-between;
      gap: 24px;
      color: #475569;
      font-size: 14px;
    }
    .sign {
      margin-top: 48px;
      padding-top: 12px;
      border-top: 1px solid #94a3b8;
      width: 220px;
    }
    @media print {
      body {
        background: white;
        padding: 0;
      }
      .sheet {
        box-shadow: none;
        border: none;
      }
    }
  </style>
</head>
<body>
  <div class="sheet">
    <div class="hero">
      <h1>Student Report Card</h1>
      <p>Student Result Management System</p>
    </div>
    <div class="content">
      <div class="grid">
        <div class="card">
          <div class="label">Student Name</div>
          <div class="value">${result.studentName}</div>
        </div>
        <div class="card">
          <div class="label">Final Grade</div>
          <div class="value"><span class="badge">${result.grade}</span></div>
        </div>
      </div>

      <table>
        <thead>
          <tr>
            <th>Subject</th>
            <th>Marks</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Subject 1</td>
            <td>${result.marks1}</td>
          </tr>
          <tr>
            <td>Subject 2</td>
            <td>${result.marks2}</td>
          </tr>
          <tr>
            <td>Subject 3</td>
            <td>${result.marks3}</td>
          </tr>
          <tr>
            <td><strong>Total</strong></td>
            <td><strong>${result.total}</strong></td>
          </tr>
        </tbody>
      </table>

      <div class="footer">
        <div>
          <div><strong>Status:</strong> ${result.grade === "C" ? "Needs improvement" : "Passed"}</div>
          <div><strong>Generated by:</strong> Student Result Management App</div>
        </div>
        <div class="sign">Authorized Signature</div>
      </div>
    </div>
  </div>
  <script>
    window.onload = function () {
      window.print();
    };
  </script>
</body>
</html>`;

  printWindow.document.open();
  printWindow.document.write(html);
  printWindow.document.close();
}
